# 公式引擎与强类型字段体系协作逻辑

## 1. 系统架构概览

Teable 的公式计算体系采用分层架构设计，各层职责清晰，通过明确的接口契约进行协作：

```
┌─────────────────────────────────────────────────────────┐
│                     应用层 (Backend)                    │
│  ┌─────────────────┐  ┌──────────────────────────────┐  │
│  │ 字段计算服务    │  │ 引用服务 / 拓扑排序服务      │  │
│  │ (FieldCalculationService) │ (ReferenceService)     │  │
│  └─────────────────┘  └──────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                            │
┌─────────────────────────────────────────────────────────┐
│                     领域层 (Core)                       │
│  ┌─────────────────┐  ┌─────────┐  ┌─────────────────┐ │
│  │ 公式字段模型    │  │ 求值器  │  │ 强类型字段体系  │ │
│  │ (FormulaField)  │  │ (EvalVisitor) │ (FieldCore) │ │
│  └─────────────────┘  └─────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────┘
                            │
┌─────────────────────────────────────────────────────────┐
│                     语法层 (Formula)                    │
│  ┌─────────────────┐  ┌──────────────────────────────┐  │
│  │ ANTLR 解析器    │  │ 字段引用提取器               │  │
│  │ (parseFormula)  │  │ (FieldReferenceVisitor)      │  │
│  └─────────────────┘  └──────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

## 2. 核心子系统分工

### 2.1 表达式解析子系统

**所在模块**: `packages/formula`

**职责**: 将用户输入的公式字符串转换为结构化的抽象语法树 (AST)。

**关键组件**:
- `parseFormula(expression: string): ExprContext`
  - 入口函数，使用 ANTLR4 词法/语法分析器构建 AST
  - 基于 `FormulaLexer` 和 `Formula` 解析器
  - 返回可被 Visitor 模式遍历的语法树节点

- `FieldReferenceVisitor`
  - 遍历 AST，提取所有字段引用 ID（`{fieldId}` 格式）
  - 用于依赖分析和循环检测

**设计要点**:
- 纯语法解析，不涉及业务语义
- 与字段类型系统解耦，仅负责语法合法性检查
- 错误通过 `FormulaErrorListener` 收集

### 2.2 强类型字段体系

**所在模块**: `packages/core/src/models/field`

**职责**: 定义所有字段类型的元数据契约，为公式引擎提供类型系统支撑。

**核心抽象**: `FieldCore` 抽象基类

**强类型属性**:
```typescript
abstract cellValueType: CellValueType;   // String / Number / Boolean / DateTime
isMultipleCellValue?: boolean;           // 是否为多值数组
dbFieldType!: DbFieldType;              // 数据库存储类型
abstract type: FieldType;               // 业务字段类型枚举
```

**公式字段实现**: `FormulaFieldCore extends FormulaAbstractCore`

**关键能力**:
1. **类型推导** (`getParsedValueType`)
   - 静态分析表达式，推导返回值的 `cellValueType` 和 `isMultipleCellValue`
   - 不依赖实际记录数据，仅通过字段元数据推导

2. **引用解析** (`getReferenceFieldIds`)
   - 调用 `FieldReferenceVisitor` 提取依赖的字段 ID 集合
   - 用于构建依赖图

3. **求值入口** (`evaluate`)
   - 接收依赖字段映射和记录数据
   - 委托给 `EvalVisitor` 执行实际计算

### 2.3 求值引擎

**所在模块**: `packages/core/src/formula`

**职责**: 遍历 AST，结合字段类型信息和记录数据，计算最终结果。

**核心组件**: `EvalVisitor`

**求值流程**:
1. **节点访问**: 递归遍历 AST 各节点（字面量、运算符、函数调用、字段引用）
2. **类型转换**: 通过 `TypedValueConverter` 进行函数参数的类型适配
3. **函数执行**: 从 `FUNCTIONS` 工厂获取函数实现并执行
4. **结果封装**: 返回 `TypedValue` 对象，携带值、类型、多值标记

**类型安全机制**:
```typescript
class TypedValue<T> {
  value: T;                          // 实际值
  type: CellValueType;               // 值类型
  isMultiple?: boolean;              // 是否多值
  field?: FieldCore;                 // 来源字段（用于类型追溯）
}
```

**字段引用处理** (`visitFieldReferenceCurly`):
- 从依赖映射中查找字段实例
- 从记录中提取原始值
- 按字段类型进行规范化（如 JSON 对象转字符串）
- 封装为 `TypedValue` 返回

### 2.4 循环依赖检测与拓扑排序

**所在模块**: `apps/nestjs-backend/src/features/calculation`

**职责**: 构建字段依赖图，检测循环引用，确定计算顺序。

**核心组件**:
- `ReferenceService`: 从数据库加载引用关系，构建有向图
- `getTopoOrders(graph: IGraphItem[]): ITopoItem[]`: 拓扑排序算法

**数据结构**:
```typescript
interface IGraphItem {
  fromFieldId: string;   // 被依赖字段
  toFieldId: string;     // 依赖字段（公式字段）
}

interface ITopoItem {
  id: string;
  dependencies: string[];
}
```

**检测机制**:
1. **图构建**: 通过递归 CTE 从 `reference` 表加载所有相关引用边
2. **DFS 遍历**: 使用三色标记法（未访问/访问中/已访问）检测环
3. **环检测**: 若遍历中遇到"访问中"状态的节点，说明存在循环引用
4. **拓扑排序**: 输出安全的计算顺序，确保被依赖项先计算

**错误处理**:
- 检测到循环时抛出 `CircularReferenceError`
- 错误信息包含完整的引用链：`A → B → C → A`

### 2.5 求值结果回写

**所在模块**: `apps/nestjs-backend/src/features/calculation`

**核心服务**: `FieldCalculationService`

**批量计算流程**:
1. **获取拓扑顺序**: `getTopoOrdersContext` 确定字段计算顺序
2. **分批加载记录**: 按 `calcChunkSize` 分页读取需要计算的记录
3. **逐行计算**: 按拓扑顺序依次计算每个字段的值
4. **批量更新**: 将计算结果写回数据库

**两种计算模式**:
1. **应用层计算** (JavaSript 求值)
   - 通过 `EvalVisitor` 在 Node.js 中计算
   - 适用于复杂函数、跨表引用等场景
   - 灵活性高但性能较低

2. **数据库生成列** (Generated Column)
   - 将公式转换为 SQL，使用 PostgreSQL 的 `GENERATED ALWAYS AS`
   - 由数据库自动维护，查询性能高
   - 受限于 SQL 函数支持范围

## 3. 端到端协作流程

### 3.1 公式字段创建流程

```
用户输入公式字符串
        ↓
[语法层] parseFormula() → AST
        ↓
[领域层] FormulaField.getReferenceFieldIds() → 依赖字段ID集合
        ↓
[领域层] FormulaField.getParsedValueType() → 推导结果类型
        ↓
[应用层] ReferenceService.getFieldGraphItems() → 构建依赖图
        ↓
[应用层] getTopoOrders() → 检测循环，确定拓扑顺序
        ↓
[应用层] FieldCalculationService → 批量计算历史数据
        ↓
[持久层] 保存字段元数据 + 计算结果
```

### 3.2 记录更新时的公式重计算

```
记录字段值变更
        ↓
[应用层] FormulaFieldService.getDependentFormulaFieldsInOrder()
        ↓
获取受影响的公式字段列表（按依赖深度排序）
        ↓
[应用层] 按拓扑顺序逐个重计算
        ↓
[领域层] FormulaField.evaluate(dependFieldMap, record)
        ↓
[求值器] EvalVisitor.visit() → TypedValue
        ↓
[持久层] 更新记录的公式字段值
```

### 3.3 查询时的公式求值

```
API 查询请求
        ↓
[查询构建器] RecordQueryBuilder
        ↓
[SQL转换层] SqlConversionVisitor 遍历公式 AST
        ↓
转换为 PostgreSQL 表达式（使用数据库函数）
        ↓
生成 SELECT 子句中的计算列
        ↓
[数据库] 执行查询，实时计算公式值
        ↓
返回查询结果
```

## 4. 关键协作接口

### 4.1 公式字段 ↔ 强类型体系

```typescript
// FormulaFieldCore 提供的类型服务
static getParsedValueType(
  expression: string,
  dependFieldMap: { [fieldId: string]: FieldCore }
): {
  cellValueType: CellValueType;
  isMultipleCellValue: boolean;
}

// 字段为公式求值提供元数据
interface FieldCore {
  cellValueType: CellValueType;
  isMultipleCellValue?: boolean;
  cellValue2String(value: unknown): string;
  item2String(value?: unknown): string;
}
```

### 4.2 求值器 ↔ 类型系统

```typescript
// EvalVisitor 依赖字段类型信息进行计算
class EvalVisitor {
  constructor(
    private dependencies: { [fieldId: string]: FieldCore },
    private record?: IRecord,
    private timeZone = 'UTC'
  ) {}

  // 访问字段引用时，使用 FieldCore 的类型信息封装结果
  visitFieldReferenceCurly(ctx: FieldReferenceCurlyContext): TypedValue {
    const field = this.dependencies[fieldId];
    return this.createTypedValueByField(field);
  }
}
```

### 4.3 拓扑排序 ↔ 引用服务

```typescript
// ReferenceService 构建图
async getFieldGraphItems(startFieldIds: string[]): Promise<IGraphItem[]>

// DFS 工具进行拓扑排序和环检测
function getTopoOrders(graph: IGraphItem[]): ITopoItem[]
function hasCycle(graphItems: IGraphItem[]): boolean
```

## 5. 设计权衡与决策

### 5.1 双重求值策略
- **应用层求值**: 完整支持所有函数，灵活但性能较低，用于批量计算
- **数据库求值**: 高性能，支持索引，但函数集受限，用于查询时计算

### 5.2 类型系统分层
- **语法层**: 无类型，仅处理字符串到 AST 的转换
- **求值层**: 动态类型，通过 `TypedValue` 携带运行时类型信息
- **字段层**: 静态类型，`cellValueType` 在字段创建时确定

### 5.3 循环检测时机
- **创建时**: 检测公式字段引入的新循环
- **更新时**: 检测字段修改可能引发的循环
- **查询时**: SQL 转换过程中再次检测，防止数据库死锁

## 6. 扩展点

1. **新字段类型**: 实现 `FieldCore` 抽象，公式引擎自动支持
2. **新函数**: 在 `FUNCTIONS` 工厂注册，同时实现 JS 和 SQL 版本
3. **新数据库**: 实现 `ITeableToDbFunctionConverter` 接口
4. **求值优化**: 缓存已解析的 AST，避免重复解析开销

---

**文档版本**: v1.0
**基于代码版本**: Teable v2 (2026-05-18)
**覆盖范围**: 公式解析、字段引用、求值计算、循环检测、结果回写
