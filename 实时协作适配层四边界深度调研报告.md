# 实时协作适配层四边界深度调研报告

## 一、客户端提交链路与 ShareDbAdapter.commit 现状

### 1.1 客户端提交完整链路

```
客户端 UI 编辑
    ↓
ShareDB Client Doc.submitOp()
    ↓
WebSocket (SockJS) 传输
    ↓
ShareDB Server 接收
    ↓
authMiddleware (connect/query)
    ↓
onSubmit 中间件（类型校验）
    ↓
ShareDB 内存 OT 合并 + 版本递增
    ↓
Pub/Sub 广播给所有订阅者
    ↓
ShareDB Adapter.getSnapshot() → 从 PostgreSQL 读快照验证
```

### 1.2 ShareDbAdapter.commit 现状

**文件位置**: `apps/nestjs-backend/src/share-db/share-db.adapter.ts:187-189`

```typescript
async commit() {
  throw new Error('Method not implemented.');
}
```

**关键发现**: `commit` 方法**未实现**。这是 ShareDB.DB 接口要求的方法，但 Teable 采用了**非持久化 OT 模式**：

| 维度 | 标准 ShareDB | Teable 实现 |
|------|-------------|------------|
| 操作存储 | commit() 持久化 ops 到 MongoDB/PostgreSQL | ❌ 不持久化 ops |
| 快照存储 | 周期性快照 + ops 回放 | ✅ 直接从 PostgreSQL 读实时快照 |
| 版本来源 | ops 版本链 | ✅ PostgreSQL `__version` 字段 |
| 历史回溯 | 可回放 ops 到任意版本 | ❌ 仅支持当前状态 |

### 1.3 非持久化模式的工作原理

ShareDB 标准生命周期：
```
客户端提交 op → commit() 存储 op → getOps() 回放 → 快照
```

Teable 生命周期：
```
客户端提交 op → 内存 OT 合并 → Pub/Sub 广播 → getSnapshot() 直接读 DB
```

**为什么这样设计**：
1. **简化架构**：PostgreSQL 作为唯一真相源，无需维护 ops 历史表
2. **一致性保障**：数据库事务保证原子性，OT 仅用于实时协作同步
3. **版本回补**：通过 `getOps()` 生成「虚拟 ops」填补客户端落后版本

### 1.4 内存中 OT 合并的限制

- ShareDB 仅在内存中维护版本向量，重启后丢失
- 客户端重连时通过 `getOps()` 从快照重建状态
- 不支持 ops 历史查询和回滚操作

---

## 二、@@v2-projection 来源豁免与 record 限制

### 2.1 onSubmit 中间件完整逻辑

**文件位置**: `apps/nestjs-backend/src/share-db/share-db.service.ts:191-226`

```typescript
private onSubmit = (context, next) => {
  // 豁免 1: submit option.source === '@@v2-projection'
  const submitSource = context.options?.source ?? context.extra?.source;
  if (submitSource === v2ProjectionSubmitSource) {
    return next();  // 直接放行
  }

  // 豁免 2: op.src 以 '@@v2-projection:' 开头
  const opSource = typeof context.op.src === 'string' ? context.op.src : '';
  if (opSource.startsWith(v2ProjectionOpSourcePrefix)) {
    return next();  // 直接放行
  }

  // 豁免 3: 非客户端流（内部服务端操作）
  if (!hasClientStream(context.agent)) {
    return next();  // 直接放行
  }

  // 客户端限制: 仅允许 Record 类型的编辑操作
  const [docType] = context.collection.split('_');
  if (docType !== IdPrefix.Record || !context.op.op) {
    return next(new Error('only record op can be committed'));
  }

  next();  // 放行合法的客户端 record op
};
```

### 2.2 三层豁免机制详解

| 豁免层级 | 匹配条件 | 使用场景 |
|---------|---------|----------|
| **第一层: option.source** | `options.source === '@@v2-projection'` | `ShareDbBackendPublisher.submitOp()` 时传入 |
| **第二层: op.src 前缀** | `op.src.startsWith('@@v2-projection:')` | `ShareDbRealtimeEngine` 生成 op 时拼接 requestId |
| **第三层: 非客户端流** | `!hasClientStream(agent)` | 无 WebSocket 连接的内部操作 |

### 2.3 来源标识的生成路径

**路径 1: ShareDbBackendPublisher（后端直连）**

```
ShareDbBackendPublisher.publish()
    ↓
doc.submitOp(..., { source: '@@v2-projection' })
    ↓
onSubmit 中间件匹配第一层豁免 → 放行
```

**路径 2: ShareDbPubSubPublisher（Pub/Sub 广播）**

```
ShareDbRealtimeEngine.applyChange()
    ↓
op.src = '@@v2-projection:' + requestId
    ↓
Pub/Sub 广播 → 接收方 onSubmit 匹配第二层豁免 → 放行
```

### 2.4 Record 限制的边界

**允许的客户端操作**:
- ✅ 记录单元格值修改（`rec_{tableId}` 集合）
- ✅ 记录创建（需走 API → 投影层路径）
- ✅ 记录删除（需走 API → 投影层路径）

**禁止的客户端操作**:
- ❌ 字段创建/修改/删除（`fld_{tableId}` 集合）
- ❌ 视图创建/修改/删除（`viw_{tableId}` 集合）
- ❌ 表属性修改（`tbl_{baseId}` 集合）
- ❌ 空操作（`!context.op.op`）

**设计意图**:
1. 结构变更（表/字段/视图）必须通过 API 层进行权限校验和业务逻辑处理
2. 仅单元格编辑这种高频、低风险操作允许直接走 OT 路径
3. 降低 OT 冲突复杂度，结构变更采用强一致 API 模式

---

## 三、批量变更在阈值处跳过逐条实时的策略

### 3.1 阈值定义与判定

**文件位置**: `packages/v2/core/src/application/projections/BatchRecordRefreshPolicy.ts`

```typescript
export const DEFAULT_LARGE_RECORD_BATCH_REFRESH_THRESHOLD = 1000;

export const shouldSkipRealtimeBatchMutation = (size: number): boolean => {
  return size >= DEFAULT_LARGE_RECORD_BATCH_REFRESH_THRESHOLD;
};
```

**判定时机**: `RecordsBatchUpdatedRealtimeProjection.handle()` 入口处

### 3.2 完整跳过流程

**文件位置**: `packages/v2/core/src/application/projections/RecordsBatchUpdatedRealtimeProjection.ts:35-62`

```
RecordsBatchUpdated 事件到达
    ↓
totalRecordCount = orchestration?.totalRecordCount ?? event.updates.length
    ↓
skipRealtime = shouldSkipRealtimeBatchMutation(totalRecordCount)
    ↓
┌─────────────────────────────────────────┐
│            skipRealtime === true        │
│  ┌───────────────────────────────────┐  │
│  │  不生成任何 RealtimeChange        │  │
│  │  不调用 realtimeEngine.applyChange│  │
│  │  直接返回 ok(undefined)           │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│            skipRealtime === false       │
│  ┌───────────────────────────────────┐  │
│  │  按记录分组 RealtimeChange        │  │
│  │  runRealtimeTasks(并发 32)        │  │
│  │  逐条调用 applyChange             │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

### 3.3 并发控制

**文件位置**: `packages/v2/core/src/application/projections/runRealtimeTasks.ts`

```typescript
export const REALTIME_TASK_CONCURRENCY_LIMIT = 32;

export async function runRealtimeTasks(
  tasks: ReadonlyArray<() => Promise<Result<void, DomainError>>>,
  concurrency = REALTIME_TASK_CONCURRENCY_LIMIT
): Promise<ReadonlyArray<Result<void, DomainError>>> {
  // 固定 32 个 worker 并发执行任务池
  // 超出部分排队等待
}
```

### 3.4 跳过策略的权衡

**阈值 1000 的选择依据**:

| 维度 | < 1000 条（逐条实时） | >= 1000 条（跳过实时） |
|------|----------------------|----------------------|
| 实时性 | ✅ 毫秒级感知 | ❌ 需等待客户端刷新 |
| 内存压力 | ❌ O(n) 内存占用，可能 GC 压力 | ✅ 恒定内存 |
| Pub/Sub 流量 | ❌ O(n) 消息 | ✅ 0 额外消息 |
| 客户端体验 | ✅ 平滑动画 | ❌ 全量刷新闪烁 |
| 典型场景 | 粘贴少量数据、批量修改小范围 | 全表导入、大数据量粘贴 |

**客户端兜底机制**:
- 跳过实时同步后，客户端通过轮询或手动刷新获取最新数据
- API 响应中包含 `shouldRefresh` 标志，前端触发全量刷新

### 3.5 边界行为

**正好等于 1000 条**: `size >= 1000` → **跳过**

**流式批处理**: `orchestration.totalChunkCount > 1` 时，每块独立判断。如果单块 >= 1000 则跳过该块。

---

## 四、getOps 版本回补与并发扇出时序

### 4.1 getOps 触发场景

ShareDB 客户端版本落后时自动调用 `getOps()` 获取缺失操作：

```
客户端本地版本: v=3
服务端当前版本: v=7
    ↓
客户端发送 getOps(from=3, to=7)
    ↓
服务端返回 4 个 op（v3, v4, v5, v6）
    ↓
客户端应用 op → 本地状态同步到 v7
```

### 4.2 虚拟 ops 生成算法

**文件位置**: `apps/nestjs-backend/src/share-db/share-db.adapter.ts:336-412`

```typescript
async internalGetOps(collection, id, from, to, options, callback, dataFunctions) {
  // 1. 从数据库读取当前版本和类型
  const { version, type } = await getVersionAndType(collectionId, id);
  
  // 2. 检查是否有版本 gap
  if (!this.hasGapVersion({ opType: type, currentVersion: version, fromVersion: from })) {
    callback(null, []);  // 无 gap，直接返回空
    return;
  }
  
  // 3. 从数据库读取最新快照
  const snapshotData = await getSnapshotData(docType, collectionId, [id], options);
  
  // 4. 根据文档类型生成 ops
  if (type === RawOpType.Create) {
    // 创建操作: 返回单个 create op
    callback(null, [{
      create: { type: 'json0', data },
      v: version,
    }]);
    return;
  }
  
  // 5. 编辑操作: 生成 gap 个空 op，最后一个携带完整快照
  const editOp = this.getOpsFromSnapshot(docType, data);
  const gapVersion = Math.max((to || version + 1) - from, 0);
  
  // 填充 gapVersion 个空 op
  const editOps = new Array(gapVersion).fill(0).map((_, i) => ({
    src: randomId,
    seq: 1,
    v: from + i,
  }));
  
  // 最后一个 op 携带完整快照的 set 操作
  if (gapVersion > 0) {
    editOps[gapVersion - 1].op = editOp;
  }
  
  callback(null, editOps);
}
```

### 4.3 快照转 ops 规则

**文件位置**: `apps/nestjs-backend/src/share-db/share-db.adapter.ts:517-550`

```typescript
private getOpsFromSnapshot(docType: IdPrefix, snapshot: unknown): IOtOperation[] {
  switch (docType) {
    case IdPrefix.Record:
      // 记录: 遍历 fields，为每个字段生成 setRecord op
      return Object.entries((snapshot as IRecord).fields).map(([fieldId, fieldValue]) => {
        return RecordOpBuilder.editor.setRecord.build({
          fieldId,
          newCellValue: fieldValue,
          oldCellValue: undefined,  // ⚠️ 无旧值，不做冲突检测
        });
      });
    case IdPrefix.Field:
      // 字段: 遍历属性，生成 setFieldProperty op
      return Object.entries(snapshot as IFieldVo)
        .filter(([key]) => key !== 'id')
        .map(([key, value]) => {
          return FieldOpBuilder.editor.setFieldProperty.build({
            key: key as IFieldPropertyKey,
            newValue: value,
            oldValue: undefined,  // ⚠️ 无旧值
          });
        });
    case IdPrefix.Table:
      // 表: 类似字段
      // ...
    default:
      return [];
  }
}
```

### 4.4 并发扇出时序分析

**场景**: 多个客户端同时修改同一文档的不同字段

```
时间线:
T0: 服务端版本 v=5，快照: { fields: { f1: 'a', f2: 'b' } }

T1: ClientA 提交 opA (修改 f1='A'), 版本 v=5
T1: ClientB 提交 opB (修改 f2='B'), 版本 v=5

T2: ShareDB 内存 OT 合并:
    - opA 先到达 → 应用成功，版本 v=6
    - opB 后到达 → 版本已变为 v=6，冲突
    - ShareDB 自动重试 opB (maxSubmitRetries=3)
    - opB 重试用 v=6 提交 → 成功，版本 v=7

T3: Pub/Sub 广播 opA 和 opB 给所有订阅者

T4: ClientA 和 ClientB 收到对方的 op → 本地合并
    最终状态: { fields: { f1: 'A', f2: 'B' } } ✓
```

**时序关键控制点**:

1. **提交重试**: `maxSubmitRetries: 3`（`share-db.service.ts:62`），版本冲突时自动重试
2. **操作顺序**: ShareDB 内存队列保证提交串行化
3. **广播顺序**: Pub/Sub 保证同一文档的 op 按提交顺序送达
4. **Gap 填充**: 客户端重连时，通过 getOps 一次性补齐所有缺失版本

### 4.5 getOpsBulk 批量回补

**文件位置**: `apps/nestjs-backend/src/share-db/share-db.adapter.ts:440-515`

```
客户端批量订阅多个文档（如打开视图加载 50 条记录）
    ↓
getOpsBulk(fromMap: { doc1: 0, doc2: 3, doc3: 5, ... })
    ↓
批量查询所有文档的版本和类型
    ↓
筛选出有 gap 的文档
    ↓
批量读取这些文档的快照
    ↓
为每个文档独立生成虚拟 ops
    ↓
返回 { doc1: [...ops], doc2: [...ops], ... }
```

**优化点**:
- 减少数据库 round-trip：一次批量查询替代 N 次单独查询
- 内存中组装每个文档的 ops，最后一次性返回
- 单个文档出错不影响其他文档（但当前实现是单个出错整体失败）

### 4.6 风险与边界

1. **无旧值冲突检测**: 回补 ops 时 `oldValue: undefined`，意味着如果客户端有未提交的本地修改，回补 op 会**静默覆盖**
2. **最后一个 op 有效**: gap 填充中只有最后一个 op 携带真实操作，前面的都是空 op。客户端应用时前面的空 op 不改变状态，最后一个 op 一步到位
3. **版本号单调递增**: 即使没有真实操作，虚拟 op 的版本号也是连续的，保证客户端版本链不断裂
4. **删除文档特殊处理**: `RawOpType.Del` 时 `hasGapVersion()` 返回 false，不生成回补 ops

---

## 五、四边界关联关系总结

```
┌─────────────────────────────────────────────────────────────────────┐
│                  客户端提交链路 (边界一)                              │
│  WebSocket → onSubmit → OT合并 → Pub/Sub                             │
│  依赖: ShareDbAdapter.commit 未实现 → 非持久化模式                   │
│                          ↓                                           │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │            @@v2-projection 豁免 (边界二)                       │  │
│  │  三层豁免机制 → 区分服务端投影操作 vs 客户端操作               │  │
│  │  客户端限制: 仅 Record 可直接提交                              │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                          ↓                                           │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │            批量变更阈值策略 (边界三)                           │  │
│  │  >= 1000 条跳过逐条实时 → 降低内存和 Pub/Sub 压力             │  │
│  │  < 1000 条并发 32 逐条应用 → 保证实时性                       │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                          ↓                                           │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │            getOps 版本回补 (边界四)                           │  │
│  │  客户端版本落后 → 从 DB 快照生成虚拟 ops → 补齐版本链         │  │
│  │  支持 bulk 批量回补 → 减少 DB round-trip                      │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 六、关键文件索引

| 边界 | 文件路径 | 关键行 |
|------|----------|--------|
| 客户端提交链路 | `apps/nestjs-backend/src/share-db/share-db.adapter.ts` | 187-189 (commit 未实现) |
| | `apps/nestjs-backend/src/share-db/share-db.service.ts` | 191-226 (onSubmit) |
| | `apps/nestjs-backend/src/ws/ws.gateway.ts` | WebSocket 网关 |
| @@v2-projection 豁免 | `apps/nestjs-backend/src/share-db/share-db.service.ts` | 22-23 (常量定义), 191-226 (校验逻辑) |
| | `packages/v2/adapter-realtime-sharedb/src/ShareDbBackendPublisher.ts` | 10-12 (option.source) |
| | `packages/v2/adapter-realtime-sharedb/src/ShareDbRealtimeEngine.ts` | 18, 146-148 (op.src 前缀) |
| 批量阈值策略 | `packages/v2/core/src/application/projections/BatchRecordRefreshPolicy.ts` | 1, 21-25 |
| | `packages/v2/core/src/application/projections/RecordsBatchUpdatedRealtimeProjection.ts` | 35-62 (跳过逻辑) |
| | `packages/v2/core/src/application/projections/runRealtimeTasks.ts` | 5 (并发限制 32) |
| getOps 版本回补 | `apps/nestjs-backend/src/share-db/share-db.adapter.ts` | 336-412 (internalGetOps) |
| | `apps/nestjs-backend/src/share-db/share-db.adapter.ts` | 440-515 (getOpsBulk) |
| | `apps/nestjs-backend/src/share-db/share-db.adapter.ts` | 517-550 (getOpsFromSnapshot) |
