# 实时协作适配层调研报告

## 一、架构概述

Teable 的实时协作适配层采用 **分层架构**，基于 ShareDB（Operational Transform 实现）构建，核心目标是在数据库变更、广播通道与多客户端之间维持数据一致性。

```
┌─────────────────────────────────────────────────────────────────┐
│                        领域层 (Domain)                          │
│  领域事件：RecordUpdated / FieldCreated / TableCreated 等       │
└─────────────────────────────────┬───────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                      投影层 (Projections)                        │
│  RealtimeProjection → 将领域事件转换为 RealtimeChange           │
│  - RecordUpdatedRealtimeProjection                               │
│  - FieldCreatedRealtimeProjection                                │
│  - RecordsBatchUpdatedRealtimeProjection                         │
└─────────────────────────────────┬───────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                    实时引擎抽象层 (Ports)                        │
│  IRealtimeEngine 接口：ensure / applyChange / delete            │
│  RealtimeChange 类型：set / insert / delete                     │
│  RealtimeDocId：collection/docId 封装                           │
└─────────────────────────────────┬───────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                  ShareDB 适配器层 (Adapter)                      │
│  ShareDbRealtimeEngine → RealtimeChange 转 json0 OT 操作        │
│  ShareDbBackendPublisher / ShareDbPubSubPublisher               │
└─────────────────────────────────┬───────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ShareDB 核心层                                │
│  - OT 操作转换与冲突合并（json0 类型）                           │
│  - Pub/Sub 广播通道（Redis 或内存）                              │
│  - 版本向量管理                                                  │
└─────────────────────────────────┬───────────────────────────────┘
                                  │
          ┌───────────────────────┴───────────────────────┐
          ▼                                               ▼
┌───────────────────────────┐                 ┌───────────────────────────┐
│    ShareDB 数据库适配器   │                 │    WebSocket 网关          │
│  ShareDbAdapter           │                 │  WsGateway (SockJS)       │
│  - 从 PostgreSQL 读取快照 │                 │  - 客户端连接管理          │
│  - 版本号管理 (__version) │                 │  - 认证中间件              │
└───────────────────────────┘                 └───────────────────────────┘
```

---

## 二、核心组件详解

### 2.1 实时引擎抽象层 (`IRealtimeEngine`)

**文件位置**: `packages/v2/core/src/ports/RealtimeEngine.ts`

```typescript
interface IRealtimeEngine {
  ensure(context, docId, initial): Promise<Result<void, DomainError>>;
  applyChange(context, docId, change, options?): Promise<Result<void, DomainError>>;
  delete(context, docId): Promise<Result<void, DomainError>>;
}
```

**设计意图**:
- 隔离核心领域与具体实时协作实现（当前为 ShareDB，未来可替换为 Yjs/CRDT 等）
- 提供统一的实时变更 API，上层投影无需关心底层 OT 细节

### 2.2 变更事件模型 (`RealtimeChange`)

**文件位置**: `packages/v2/core/src/ports/RealtimeChange.ts`

```typescript
type RealtimeChange =
  | { type: 'set'; path: RealtimePath; value: unknown; oldValue?: unknown }
  | { type: 'insert'; path: RealtimePath; index: number; value: unknown }
  | { type: 'delete'; path: RealtimePath; index: number; count: number };
```

**语义说明**:
- `set`: 对象属性替换，支持 `oldValue` 用于冲突检测
- `insert`: 列表插入
- `delete`: 列表删除（从后向前删除以保持索引有效）

### 2.3 文档标识 (`RealtimeDocId`)

**文件位置**: `packages/v2/core/src/ports/RealtimeDocId.ts`

格式: `{collection}/{docId}`，例如 `rec_tbl123/rec456`

**集合命名规则**:
- 记录: `rec_{tableId}`
- 字段: `fld_{tableId}`
- 视图: `viw_{tableId}`
- 表: `tbl_{baseId}`

---

## 三、事件转换流程

### 3.1 领域事件 → RealtimeChange（投影层）

**文件位置**: `packages/v2/core/src/application/projections/`

以 `RecordUpdatedRealtimeProjection` 为例:

```
领域事件 RecordUpdated
    ↓
  解析: tableId, recordId, changes[{fieldId, oldValue, newValue}]
    ↓
  构建 RealtimeDocId: rec_{tableId}/{recordId}
    ↓
  转换为 RealtimeChange:
    { type: 'set', path: ['fields', fieldId], value: newValue, oldValue }
    ↓
  调用 realtimeEngine.applyChange(..., { version: event.oldVersion })
```

**关键设计**:
- 每个领域事件类型对应一个 Projection Handler（单一职责）
- 版本号 `oldVersion` 从领域事件携带，用于 OT 排序
- 附件值需经过 `decorateRealtimeAttachmentValue` 处理（签名 URL）

### 3.2 RealtimeChange → ShareDB json0 操作

**文件位置**: `packages/v2/adapter-realtime-sharedb/src/ShareDbRealtimeEngine.ts:93-118`

转换规则:

| RealtimeChange 类型 | ShareDB json0 操作 | 说明 |
|-------------------|-------------------|------|
| `set` (有 oldValue) | `{ p: path, oi: newValue, od: oldValue }` | 对象替换，含旧值用于冲突检测 |
| `set` (无 oldValue) | `{ p: path, oi: newValue }` | 对象替换，无旧值 |
| `insert` | `{ p: [...path, index], li: value }` | 列表插入 |
| `delete` | 多个 `{ p: [...path, index+i], ld: true }` | 列表删除（倒序） |

**发布通道**:
- 集合级通道: `[collection]`
- 文档级通道: `[collection.docId]`

---

## 四、状态合并与一致性保证

### 4.1 ShareDB OT 合并机制

ShareDB 使用 **json0** 类型的 Operational Transform，核心特性:

1. **并发控制**: 基于版本号 `v` 排序操作
2. **变换函数**: 自动转换并发操作的位置偏移
3. **提交重试**: `maxSubmitRetries: 3`（`share-db.service.ts:62`）

**测试验证** (`ShareDbRealtimeEngine.spec.ts:359-400`):
```
初始状态: { title: 'Hi' }
ClientA: 在位置 0 插入 'A' → { p: ['title', 0], si: 'A' }
ClientB: 在位置 2 插入 'B' → { p: ['title', 2], si: 'B' }
合并结果: { title: 'AHiB' } ✓
```

### 4.2 版本号管理

**来源**: 数据库 `__version` 字段（`record-readonly.service.ts:106-108`）

```sql
SELECT __version as version FROM {table} WHERE __id = {recordId}
```

**传递路径**:
```
PostgreSQL __version
    ↓
  领域事件 oldVersion
    ↓
  RealtimeApplyChangeOptions.version
    ↓
  ShareDB Op.v
```

### 4.3 快照读取机制

**文件位置**: `apps/nestjs-backend/src/share-db/share-db.adapter.ts`

`ShareDbAdapter` 实现了 ShareDB.DB 接口，关键方法:
- `getSnapshot()` / `getSnapshotBulk()`: 从 PostgreSQL 读取最新快照
- `getOps()`: 当客户端版本落后时，生成「虚拟操作序列」填补 gap
- `query()`: 支持带过滤条件的文档查询

**Gap 填充逻辑** (`internalGetOps`, line 336-412):
```
客户端请求版本: from=2, to=5
数据库当前版本: v=5
→ 生成 gapVersion = 5 - 2 = 3 个空操作
→ 最后一个操作携带完整快照的 set 操作
→ 客户端应用后状态与数据库一致
```

---

## 五、广播通道与客户端同步

### 5.1 Pub/Sub 架构

**文件位置**: `apps/nestjs-backend/src/share-db/share-db.service.ts:65-74`

```
┌─────────────┐
│  服务端操作  │  →  ShareDbBackendPublisher.submitOp()
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  ShareDB    │  →  Pub/Sub 广播
│  内存 / Redis│
└──────┬──────┘
       │
       ├─────────────────────────┐
       ▼                         ▼
┌───────────────┐       ┌───────────────┐
│  WebSocket    │       │  WebSocket    │
│  Client A     │       │  Client B     │
└───────────────┘       └───────────────┘
```

**Redis 模式**: 当缓存配置为 `redis` 时启用，支持多实例部署

### 5.2 事务后广播钩子

**文件位置**: `apps/nestjs-backend/src/share-db/share-db.service.ts:80-101`

```typescript
this.prismaService.bindAfterTransaction(async () => {
  const rawOpMaps = this.cls.get('tx.rawOpMaps');
  if (rawOpMaps?.length) {
    await this.updateTableMetaByRawOpMap(rawOpMaps);
    await this.publishOpsMap(rawOpMaps);
    this.eventEmitterService.ops2Event(ops);
  }
});
```

**设计要点**:
- `rawOpMaps` 存储在 CLS（Continuation Local Storage）事务上下文中
- 事务提交后统一广播，避免部分成功部分失败
- 广播失败不影响数据库事务已提交的结果

### 5.3 WebSocket 网关

**文件位置**: `apps/nestjs-backend/src/ws/ws.gateway.ts`

**传输层**: SockJS，支持:
- `websocket`: 首选传输
- `xhr-streaming`: 降级方案
- 响应限制: 2MB（处理批量操作）

**认证流程**:
```
客户端连接 → 提取 Cookie/ShareId → authMiddleware
    ↓
  agent.custom 绑定: userId, cookie, shareId, baseShareId
    ↓
  后续查询/提交操作携带认证上下文
```

---

## 六、冲突处理边界

### 6.1 允许客户端提交的操作类型

**文件位置**: `share-db.service.ts:191-226` (onSubmit 中间件)

```typescript
if (docType !== IdPrefix.Record || !context.op.op) {
  return next(new Error('only record op can be committed'));
}
```

**规则**:
- ✅ 允许: 记录（Record）的编辑操作
- ❌ 禁止: 表/字段/视图的客户端直接提交（必须通过 API → 投影层）

### 6.2 服务端操作豁免

服务端发起的操作通过 `source` 标识跳过客户端限制:
- `@@v2-projection`: V2 投影层来源 (`ShareDbBackendPublisher.ts:10-12`)
- `@@v2-projection:{requestId}`: 带请求 ID 的来源

### 6.3 冲突解决策略

**ShareDB 默认策略**: **先提交者胜（First Write Wins）**

- 客户端 A 提交 op 版本 v=3 → 成功，版本变为 v=4
- 客户端 B 同时提交 op 版本 v=3 → 失败，ShareDB 自动重试 3 次
- 重试时自动变换 op 的位置/版本号

**边界情况处理**:

1. **大批次更新跳过实时同步**
   - 阈值: `DEFAULT_LARGE_RECORD_BATCH_REFRESH_THRESHOLD = 1000`
   - 超过阈值的批量操作不发送 per-record op，改为客户端整体刷新

2. **操作来源隔离**
   - 客户端 op: 经过完整 OT 合并
   - 投影层 op: 标记为服务端来源，优先应用

3. **文档不存在时的创建**
   - 删除操作时若文档不存在，先创建空文档再删除
   - 创建操作时若文档已存在，静默跳过

---

## 七、双向数据流完整链路

### 7.1 客户端修改 → 服务端 → 其他客户端

```
ClientA 本地修改
    ↓
  ShareDB Client Doc.submitOp()
    ↓
  WebSocket → ShareDB Server
    ↓
  onSubmit 中间件校验（仅允许 Record op）
    ↓
  ShareDB OT 合并 + 版本递增
    ↓
  Pub/Sub 广播到所有订阅者
    ↓
  ClientB 收到 op → 本地应用 → UI 更新
```

### 7.2 服务端数据库变更 → 所有客户端

```
API 请求 → Command Handler
    ↓
  数据库更新（__version 递增）
    ↓
  发布领域事件（RecordUpdated 等）
    ↓
  RealtimeProjection 接收事件 → 转换为 RealtimeChange
    ↓
  ShareDbRealtimeEngine.applyChange()
    ↓
  ShareDbBackendPublisher.submitOp()
    ↓
  Pub/Sub 广播 → 所有客户端应用
```

---

## 八、关键设计决策与权衡

| 决策 | 优点 | 缺点 |
|------|------|------|
| **ShareDB (OT) 而非 CRDT** | 1. 中心化权威状态<br>2. 操作语义明确<br>3. 与现有 PostgreSQL 集成好 | 1. 服务端压力大<br>2. 离线编辑支持弱 |
| **仅允许 Record 客户端提交** | 1. 降低冲突复杂度<br>2. 结构变更走 API 受控 | 1. 字段/表修改实时性稍差<br>2. 增加 API 调用 |
| **数据库作为唯一真相源** | 1. 数据一致性有保障<br>2. 快照可从 DB 重建 | 1. 快照读取有延迟<br>2. DB 成为瓶颈 |
| **Post-commit 广播** | 1. 事务原子性不被破坏<br>2. 广播失败不影响数据 | 1. 极端情况客户端可能未收到通知<br>2. 需依赖查询轮询兜底 |
| **大批次跳过实时同步** | 1. 避免内存压力<br>2. 减少 Pub/Sub 流量 | 1. 批量更新时客户端体验下降 |

---

## 九、潜在改进点

1. **操作压缩**: 频繁小操作可批量合并后广播
2. **离线支持**: 当前架构依赖实时连接，可考虑引入本地队列
3. **冲突可观测性**: 增加冲突发生时的 metrics 上报
4. **优雅降级**: Pub/Sub 故障时切换到轮询模式
5. **操作验证**: 客户端 op 提交前增加权限校验中间件

---

## 十、参考文件清单

| 模块 | 文件路径 |
|------|----------|
| 实时引擎接口 | `packages/v2/core/src/ports/RealtimeEngine.ts` |
| ShareDB 适配器 | `packages/v2/adapter-realtime-sharedb/src/ShareDbRealtimeEngine.ts` |
| 记录更新投影 | `packages/v2/core/src/application/projections/RecordUpdatedRealtimeProjection.ts` |
| ShareDB 服务 | `apps/nestjs-backend/src/share-db/share-db.service.ts` |
| 数据库适配器 | `apps/nestjs-backend/src/share-db/share-db.adapter.ts` |
| WebSocket 网关 | `apps/nestjs-backend/src/ws/ws.gateway.ts` |
| 认证中间件 | `apps/nestjs-backend/src/share-db/auth.middleware.ts` |
| 单元测试 | `packages/v2/adapter-realtime-sharedb/src/ShareDbRealtimeEngine.spec.ts` |
| 集成测试 | `apps/nestjs-backend/test/collaboration.e2e-spec.ts` |
