# 实时协作适配层边界调研报告

## 调研对象：Teable v2 实时协作适配层四个关键边界
**重点：客户端提交链路、来源豁免机制、批量刷新策略、版本回补与并发扇出

---

## 边界一：客户端提交链路与 ShareDbAdapter.commit 现状

### 1.1 ShareDbAdapter.commit 未实现

**文件位置**: `apps/nestjs-backend/src/share-db/share-db.adapter.ts:187-189

```typescript
async commit() {
  throw new Error('Method not implemented.');
}
```

**核心结论**：ShareDB 适配器的 `commit` 方法**未实现**，直接抛出错误。

### 1.2 客户端提交完整链路

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        客户端 (Browser / SockJS 连接                    │
│  Doc.submitOp(op)                                               │
└─────────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    WebSocket 网关 (WsGateway)                             │
│  SockJS 传输层                                                │
└─────────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    ShareDB 服务端 (ShareDbService)              │
│  authMiddleware 认证校验                                            │
│  onSubmit 中间件校验（仅 Record 类型检查）                            │
└─────────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    ShareDB 核心 OT 合并                            │
│  OT 操作合并 + 版本递增（v++）                                   │
└─────────────────────────────────────────────────────────────────────────┘
                                  │
          ┌─────────────────────────┴─────────────────────────┐
          ▼                                               ▼
┌───────────────────────────┐                     ┌───────────────────────────┐
│   Pub/Sub 广播         │                     │   尝试 commit()          │
│  (Redis / 内存)         │                     │   → 抛出 Error        │
│  → 所有订阅客户端       │                     │   → 操作不持久化       │
└───────────────────────────┘                     └───────────────────────────┘
```

### 1.3 数据库作为唯一真相源

由于 `commit` 未实现，ShareDB **不承担持久化责任**。数据库才是唯一真相源：

- **客户端编辑** → 只负责：
  1. API → Command Handler → PostgreSQL 更新（__version 递增）→ 领域事件 → RealtimeProjection → ShareDB 广播
- **客户端提交** → 只负责：
  1. 本地 UI 乐观更新（本地 Doc 数据 + 提交到 ShareDB
  2. ShareDB 内存状态
  3. ShareDB Pub/Sub 广播给其他客户端
  4. **不写入数据库
  5. **数据库更新通过独立 API 调用完成（独立的命令）

### 1.4 客户端提交的操作数据与数据库更新的解耦设计

```
客户端编辑单元格
    ↓
ShareDB Client Doc.submitOp()
    ↓
ShareDB Server 接收 + Pub/Sub 广播 → 其他客户端实时看到更新
    ↓
（同时）API 调用 /record/update API （独立的 HTTP 请求）
    ↓
PostgreSQL 更新 __version
    ↓
领域事件 → RealtimeProjection
    ↓
（可重复 op（可能重复）
```

**风险点**：
- 客户端提交到 ShareDB 的操作和 API 调用是两条独立链路
- 存在时序不一致风险：客户端 op 先到，API 后到
- ShareDB 内存状态可能与数据库状态存在短暂不一致

---

## 边界二：@@v2-projection 来源豁免与 record 限制

### 2.1 onSubmit 中间件校验逻辑

**文件位置**: `apps/nestjs-backend/src/share-db/share-db.service.ts:191-226`

```typescript
private onSubmit = (context, next) => {
  // 检查 1: submitSource === '@@v2-projection' → 放行
  const submitSource = context.options?.source ?? context.extra?.source;
  if (submitSource === v2ProjectionSubmitSource) {
    return next();
  }

  // 检查 2: opSource 以 '@@v2-projection:' 开头 → 放行
  const opSource = typeof context.op.src === 'string' ? context.op.src : '';
  if (opSource.startsWith(v2ProjectionOpSourcePrefix)) {
    return next();
  }

  // 检查 3: 无客户端 stream → 放行（服务端内部操作）
  if (!hasClientStream(context.agent)) {
    return next();
  }

  // 检查 4: 客户端提交 → 必须是 Record 类型且有 op
  const [docType] = context.collection.split('_');
  if (docType !== IdPrefix.Record || !context.op.op) {
    return next(new Error('only record op can be committed'));
  }
  next();
};
```

### 2.2 来源标识定义

```typescript
const v2ProjectionOpSourcePrefix = '@@v2-projection:';
const v2ProjectionSubmitSource = '@@v2-projection';
```

### 2.3 三重豁免 + 一层限制

| 检查层级 | 条件 | 结果 |
|---------|------|------|
| 1 | submitSource === '@@v2-projection | 直接放行 |
| 2 | op.src 以 '@@v2-projection:' 开头 | 直接放行 |
| 3 | agent 无 stream（非客户端连接） | 直接放行 |
| 4 | 客户端连接 + 非 Record 类型或无 op | 拒绝 |

### 2.4 各集合类型提交权限表

| 集合类型 | 客户端提交 | 服务端投影提交 | 说明 |
|---------|----------|--------------|------|
| Record (rec_{tableId}) | ✅ 允许 | ✅ 允许 | 单元格编辑、行创建、行删除 |
| Field (fld_{tableId}) | ❌ 禁止 | ✅ 允许 | 字段创建、字段属性修改 |
| View (viw_{tableId}) | ❌ 禁止 | ✅ 允许 | 视图创建、视图筛选排序 |
| Table (tbl_{baseId}) | ❌ 禁止 | ✅ 允许 | 表创建、表属性修改 |

### 2.5 服务端投影提交方式

**文件位置**: `packages/v2/adapter-realtime-sharedb/src/ShareDbBackendPublisher.ts:10-12

```typescript
const projectionSubmitOptions = {
  source: '@@v2-projection',
};
```

服务端投影提交时携带 `source: '@@v2-projection'，通过检查 1 豁免。

---

## 边界三：批量变更在阈值处跳过逐条实时的策略

### 3.1 阈值定义

**文件位置**: `packages/v2/core/src/application/projections/BatchRecordRefreshPolicy.ts`

```typescript
export const DEFAULT_LARGE_RECORD_BATCH_REFRESH_THRESHOLD = 1000;

export const shouldSkipRealtimeBatchMutation = (
  size: number,
  _orchestration?: IRealtimeBatchMutationOrchestrationLike
): boolean => {
  return size >= DEFAULT_LARGE_RECORD_BATCH_REFRESH_THRESHOLD;
};
```

**核心规则**: `>= 1000 条 → 跳过逐条实时同步

### 3.2 跳过策略的应用位置

| 投影类 | 文件位置 | 跳过逻辑 |
|--------|----------|----------|
| RecordsBatchCreatedRealtimeProjection | line 42, 51-59 | skipRealtime → 直接返回 ok(undefined) |
| RecordsBatchUpdatedRealtimeProjection | line 42, 54-62 | skipRealtime → 直接返回 ok(undefined) |
| RecordsDeletedRealtimeProjection | line 38, 45-53 | skipRealtime → 直接返回 ok(undefined) |

**跳过逻辑代码示例（RecordsBatchUpdatedRealtimeProjection.ts:51-62):

```typescript
if (skipRealtime) {
  await withRealtimeFanoutSpan(
    context,
    teableSpanName('teable.RecordsBatchUpdatedRealtimeProjection.realtimeFanout'),
    fanoutAttributes,
    async () => ok(undefined)
  );
  return ok(undefined);
}
```

### 3.3 跳过后的替代机制：Action Trigger 广播

**文件位置**: `apps/nestjs-backend/src/features/v2/v2-action-trigger.service.ts`

当跳过实时同步时，ActionTriggerProjection 发出 action trigger:

```typescript
// RecordsBatchCreated:
emitActionTrigger(this.shareDbService, event.tableId.toString(), [
  {
    actionKey: 'addRecord',
    payload: {
      tableId,
      recordIds,
      skipRealtime: true,
      operationId,
      groupId,
      totalRecordCount,
      totalChunkCount,
      chunkIndex,
      scope,
    },
  },
]);
```

### 3.4 客户端处理 skipRealtime 逻辑

**文件位置**: `packages/sdk/src/context/use-instances/useInstances.ts:357-396

```typescript
const shouldRefreshAfterProjectedMutation = (
  tableId, batch, actionKey) => {
  for (const item of batch) {
    const payload = toLargeProjectedMutationPayload(tableId, action.payload);
    if (!payload) continue;

    // 分块处理：仅最后一块才触发刷新
    if (typeof payload.chunkIndex === 'number' &&
        typeof payload.totalChunkCount === 'number' &&
        payload.totalChunkCount > 0) {
      if (payload.chunkIndex === payload.totalChunkCount - 1) {
        return true;  // 最后一块 → 刷新
      }
      continue;  // 非最后一块 → 不刷新
    }
    return true;  // 无分块信息 → 立即刷新
  }
  return false;
};
```

### 3.5 分块刷新时序（chunk）示例

```
场景：导入 2500 条记录，每块 1000 条
→ 分 3 块：chunk 0 (1000), chunk 1 (1000), chunk 2 (500)

Chunk 0 arrive → skipRealtime: true, chunkIndex: 0, totalChunkCount: 3
→ 客户端收到，客户端：chunkIndex (0) !== totalChunkCount - 1 (2) → 不刷新，累计记录 ID

Chunk 1 arrive → skipRealtime: true, chunkIndex: 1, totalChunkCount: 3
→ 客户端：chunkIndex (1) !== totalChunkCount - 1 (2) → 不刷新，累计记录 ID

Chunk 2 arrive → skipRealtime: true, chunkIndex: 2, totalChunkCount: 3
→ 客户端：chunkIndex (2) === totalChunkCount - 1 (2) → 触发查询刷新
→ 从服务器获取完整查询刷新
```

### 3.6 删除记录的特殊处理

**文件位置**: `useInstances.ts:284-340`

对于 deleteRecord action trigger 处理：
```typescript
const getProjectedDeleteRecordIds = (tableId, batch) => {
  // 收集所有 skipRealtime: true 的删除记录 ID
  // 直接从 reducer 中移除
```

删除记录无需等待最后一块，每块到达就立即从本地 reducer 移除。

---

## 边界四：getOps 版本回补与并发扇出时序

### 4.1 getOps 调用时机

当客户端连接或重连时，ShareDB 调用 `getOps(collection, id, from, to)` 获取缺失操作历史。

### 4.2 internalGetOps 核心逻辑

**文件位置**: `apps/nestjs-backend/src/share-db/share-db.adapter.ts:336-412`

```typescript
async internalGetOps(collection, id, from, to, options, callback, dataFunctions) {
  const { version, type } = await getVersionAndType(collectionId, id);

  // 条件 1: 无 gap → 返回空数组
  if (!this.hasGapVersion({ opType: type, currentVersion: version, fromVersion: from })) {
    callback(null, []);
    return;
  }

  // 条件 2: 删除操作 → 返回空数组
  if (type === RawOpType.Del) {
    callback(null, []);
    return;
  }

  // 从数据库读取最新快照
  const snapshotData = await getSnapshotData(...);
  const { data } = snapshotData[0];

  // 条件 3: 新建文档 → 返回 create 操作
  if (type === RawOpType.Create) {
    callback(null, [{ create: { type: 'json0', data }]);
    return;
  }

  // 条件 4: 编辑文档 → 生成 gapVersion 个空操作
  const editOp = this.getOpsFromSnapshot(docType, data);
  const gapVersion = Math.max((to || baseRaw.v + 1) - from, 0);

  // 生成 gapVersion 个空操作，最后一个携带完整快照
  const editOps = new Array(gapVersion).fill(0).map((_, i) => {
    return {
      ...baseRaw,
      src: getRandomString(21),
      v: from + i,
    };
  });
  if (gapVersion > 0) {
    editOps[gapVersion - 1].op = editOp;
  }
  callback(null, editOps);
}
```

### 4.3 版本回补机制详解

**场景**：客户端版本 v=2，数据库当前版本 v=5

```
客户端请求：from=2, to=null
数据库版本：version=5
gapVersion = 5 - 2 = 3

生成 3 个操作：
op1: v=2, op=[] (空)
op2: v=3, op=[] (空)
op3: v=4, op=完整快照 set 操作
```

**设计意图**：
- 客户端应用这 3 个操作后，本地版本从 2 递增到 5
- 最后一个操作携带完整快照数据覆盖本地状态

### 4.4 getOpsFromSnapshot 快照展开

**文件位置**: `share-db.adapter.ts:517-550

```typescript
private getOpsFromSnapshot(docType, snapshot) {
  switch (docType) {
    case IdPrefix.Record:
      return Object.entries(snapshot.fields).map(([fieldId, fieldValue]) => {
        return RecordOpBuilder.editor.setRecord.build({
          fieldId,
          newCellValue: fieldValue,
          oldCellValue: undefined,
        });
      });
    case IdPrefix.Field:
      return Object.entries(snapshot)
        .filter(([key]) => key !== 'id')
        .map(([key, value]) => {
          return FieldOpBuilder.editor.setFieldProperty.build({
            key, newValue: value, oldValue: undefined,
          });
        });
    // ...
  }
}
```

**关键**：所有字段的 `oldValue` 都为 `undefined`，这意味着：
- 这是一个「强制覆盖」操作，不是「条件替换」
- 不携带旧值，不会进行冲突检测

### 4.5 并发扇出控制

**文件位置**: `packages/v2/core/src/application/projections/runRealtimeTasks.ts

```typescript
export const REALTIME_TASK_CONCURRENCY_LIMIT = 32;

export async function runRealtimeTasks(tasks, concurrency = 32) {
  const workerCount = Math.min(normalizedConcurrency, tasks.length);
  await Promise.all(Array.from({ length: workerCount }, () => worker()));
}
```

### 4.6 并发扇出时序分析

**场景**：批量更新 100 条记录

```
领域事件 RecordsBatchUpdated
    ↓
RecordsBatchUpdatedRealtimeProjection
    ↓
为每条记录创建一个任务：realtimeEngine.applyChange(...)
    ↓
runRealtimeTasks 并发 32 个 worker 执行
    ↓
Worker 1: rec1, rec2, rec3, ...
Worker 2: rec33, rec34, ...
...
Worker 32: rec97, rec98, rec99, rec100
    ↓
每个任务独立调用 ShareDbBackendPublisher.publish()
    ↓
每个 publish 独立的 op 独立的 op 独立提交到 ShareDB
```

**时序特性**：
1. **并发无序**：32 个 worker 同时提交，op 到达 ShareDB 的顺序不确定
2. **版本有序**：ShareDB 内部通过版本号保证每个 op 的版本保证最终有序
3. **最终一致**：每个 record 的版本递增保证有序
4. **广播无序**：客户端收到的 op 顺序可能与数据库提交顺序可能不一致

### 4.7 并发扇出追踪

**文件位置**: `packages/v2/core/src/application/projections/traceRealtimeFanout.ts

```typescript
export const buildRealtimeFanoutSpanAttributes = (params) => ({
  'teable.total_record_count': params.totalRecordCount,
  'teable.chunk_record_count': params.chunkRecordCount,
  'teable.total_chunk_count': params.orchestration?.totalChunkCount ?? 1,
  'teable.chunk_index': params.orchestration?.chunkIndex ?? 0,
  'teable.batch_scope': params.orchestration?.scope ?? 'operation',
  'teable.skip_realtime': params.skipRealtime,
  'teable.fanout_count': params.fanoutCount,
});
```

### 4.8 潜在的时序风险点

1. **getOps 与并发 op 冲突**：
   - 客户端重连时，getOps 返回的快照 op 可能与正在飞行中的 op 冲突
   - ShareDB 会自动处理 OT 变换

2. **版本跳跃风险**：
   - 当 gapVersion 较大时，getOpsFromSnapshot 生成的空 op 不携带实际变更历史
   - 客户端应用后可能丢失中间变更历史

3. **并发扇出顺序**：
   - 同批次内的 record op 顺序不保证按 record 顺序
   - 但每个 record 内部的版本递增保证有序

---

## 总结

| 边界 | 核心机制 | 关键风险 |
|------|----------|----------|
| **客户端提交链路 | ShareDB commit 未实现，数据库是唯一真相源 | 客户端 op 与 API 更新是两条独立链路，存在时序不一致风险 |
| **来源豁免** | 三重豁免检查，仅 Record 允许客户端提交 | Field/View/Table 必须通过投影层提交 |
| **批量刷新阈值 | >= 1000 条跳过逐条实时同步，改为 action trigger 分块刷新 | 分块刷新最后一块才触发查询刷新，删除立即移除 |
| **版本回补** | getOps 生成 gapVersion 个空操作，最后一个携带完整快照 | 空 op 不携带变更历史，并发扇出无序 |
